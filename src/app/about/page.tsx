import type { Metadata } from "next";import Header from "@/components/Header";import styles from "../content.module.css";export const metadata:Metadata={title:"About"};
export default function About(){return <main className={styles.shell}><Header/><header className={styles.hero}><p className={styles.kicker}>About the hunt</p><h1>Nine dots. One shared, finite record.</h1><p>Untraced is a communal attempt to discover every valid Android-style pattern that visits all nine dots exactly once.</p></header><section className={styles.section}><h2>The rule</h2><p>Start anywhere and visit every dot without revisiting one. A line may cross the middle dot of a row, column, or long diagonal only after that midpoint has already been visited. While drawing here, crossing an unused midpoint acquires it automatically, as Android’s lock screen does.</p><p>Direction matters. Reversals, rotations, and reflections are separate routes. Exhaustive enumeration produces exactly 140,704 valid routes.</p></section><section className={styles.section}><h2>Privacy</h2><p>No account is required. This browser receives a random local identifier used for best-effort personal totals; it is a device/browser count, never a claim about unique people. Your local history and sound preference remain in browser storage. The server stores accepted submissions, the random identifier, and an optional nickname. It does not fingerprint your device or store raw unlock credentials.</p><p>Never draw a pattern you use on a real device.</p></section><section className={styles.section}><h2>Why “Untraced”?</h2><p>You draw without an answer key. Only after the line is complete does the shared record say whether anyone arrived there before you. Undiscovered routes are never exposed.</p></section></main>}
