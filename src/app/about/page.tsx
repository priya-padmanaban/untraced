import type { Metadata } from "next";import Header from "@/components/Header";import styles from "../content.module.css";export const metadata:Metadata={title:"Rules"};
export default function About(){return <main className={styles.shell}><Header/><header className={styles.hero}><p className={styles.kicker}>How it works</p><h1>140,704 possible answers.</h1><p>Draw a valid nine-dot pattern. The database tells you whether it’s new.</p></header><section className={styles.section}><h2>Rules</h2><p>Start anywhere. Visit all nine dots once. If a move crosses the middle of a row, column, or long diagonal, that middle dot must already be active. The board handles this automatically.</p><p>Direction matters. Reverse, rotated, and reflected patterns are separate answers.</p></section><section className={styles.section}><h2>Data</h2><p>No account is required. A random browser ID tracks your totals, and your personal history stays in local storage. The server keeps accepted entries and an optional nickname. No fingerprinting, location, email, or advertising IDs.</p></section><section className={styles.section}><h2>No answer key</h2><p>Missing patterns are never published. You only learn whether an entry is new after submitting all nine dots.</p></section></main>}
