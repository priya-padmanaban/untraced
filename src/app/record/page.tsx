import type { Metadata } from "next";import Header from "@/components/Header";import MiniPattern from "@/components/MiniPattern";import { formatPercent } from "@/lib/patterns";import { progress } from "@/server/store";import styles from "../content.module.css";
export const metadata:Metadata={title:"The Record"};export const dynamic="force-dynamic";
const duration=(start:string)=>{const hours=Math.max(0,(Date.now()-new Date(start).getTime())/36e5);return hours<48?`${Math.floor(hours)} hours`:`${Math.floor(hours/24)} days`};
export default async function Record(){const d=await progress();return <main className={styles.shell}><Header/><header className={styles.hero}><p className={styles.kicker}>The communal record</p><h1>{d.discovered.toLocaleString()} marks, held in common.</h1><p>{formatPercent(d.discovered)} of all valid nine-node routes have been found since the hunt began {duration(d.huntStartedAt)} ago.</p></header><div className={styles.metrics}><div className={styles.metric}><span>Discovered</span><strong>{d.discovered.toLocaleString()}</strong></div><div className={styles.metric}><span>Accepted traces</span><strong>{d.totalSubmissions.toLocaleString()}</strong></div><div className={styles.metric}><span>Browsers / devices</span><strong>{d.browsers.toLocaleString()}</strong></div></div><section className={styles.section}><h2>Recent discoveries</h2>{d.recent.length?<div className={styles.gallery}>{d.recent.map((p,i)=><div className={styles.pattern} key={`${p.firstDiscoveredAt}-${i}`}><MiniPattern route={p.route}/><p>{p.name||"Anonymous tracer"} · {new Intl.DateTimeFormat("en",{dateStyle:"medium"}).format(new Date(p.firstDiscoveredAt))}</p></div>)}</div>:<p className={styles.empty}>The first discovery has not been recorded yet.</p>}</section><section className={styles.section}><h2>Most traced</h2>{d.popular.length?<div className={styles.gallery}>{d.popular.map((p,i)=><div className={styles.pattern} key={`${p.firstDiscoveredAt}-${i}`}><MiniPattern route={p.route}/><p>{p.count.toLocaleString()} accepted traces</p></div>)}</div>:<p className={styles.empty}>Frequency will appear after routes are submitted.</p>}</section><section className={styles.section}><h2>Milestones</h2>{d.milestones.length?<ol className={styles.milestones}>{d.milestones.map(m=><li key={m.threshold}><strong>{m.threshold}</strong><span>{new Intl.DateTimeFormat("en",{dateStyle:"long",timeStyle:"short"}).format(new Date(m.reachedAt))}</span></li>)}</ol>:<p className={styles.empty}>No milestone has been reached.</p>}</section></main>}
