import type { Metadata } from "next";import Header from "@/components/Header";import YouClient from "@/components/YouClient";import styles from "../content.module.css";export const metadata:Metadata={title:"My Stats"};export default function You(){return <main className={styles.shell}><Header/><header className={styles.hero}><p className={styles.kicker}>This browser</p><h1>Your numbers.</h1><p>Stored locally. No account required.</p></header><YouClient/></main>}
