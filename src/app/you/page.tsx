import type { Metadata } from "next";import Header from "@/components/Header";import YouClient from "@/components/YouClient";import styles from "../content.module.css";export const metadata:Metadata={title:"Your Marks"};export default function You(){return <main className={styles.shell}><Header/><header className={styles.hero}><p className={styles.kicker}>Your marks</p><h1>A small record kept in this browser.</h1><p>Local history is a convenience, not an account. Clearing browser storage erases it.</p></header><YouClient/></main>}
