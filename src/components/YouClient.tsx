/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import { useEffect,useState } from "react";import MiniPattern from "./MiniPattern";import styles from "@/app/content.module.css";type Entry={route:number[];new:boolean;at:string;count:number};
export default function YouClient(){const[history,setHistory]=useState<Entry[]>([]),[name,setName]=useState("");useEffect(()=>{setHistory(JSON.parse(localStorage.getItem("untraced-history")||"[]"));setName(localStorage.getItem("untraced-nickname")||"")},[]);const discoveries=history.filter(x=>x.new);return <><div className={styles.metrics}><div className={styles.metric}><span>Entries</span><strong>{history.length}</strong></div><div className={styles.metric}><span>New finds</span><strong>{discoveries.length}</strong></div><div className={styles.metric}><span>Unique</span><strong>{new Set(history.map(x=>x.route.join(""))).size}</strong></div></div><section className={styles.section}><h2>Nickname</h2><p>Used on future first finds. Maximum 24 characters.</p><form className={styles.form} onSubmit={e=>{e.preventDefault();if(!name||/^[\p{L}\p{N} _.'-]{1,24}$/u.test(name.trim()))localStorage.setItem("untraced-nickname",name.trim())}}><input aria-label="Nickname" maxLength={24} value={name} onChange={e=>setName(e.target.value)} placeholder="Anonymous"/><button className={styles.button}>Save</button></form></section><section className={styles.section}><h2>New finds</h2>{discoveries.length?<div className={styles.history}>{discoveries.map((e,i)=><div className={styles.pattern} key={`${e.at}-${i}`}><MiniPattern route={e.route}/><p>{new Intl.DateTimeFormat(undefined,{dateStyle:"medium"}).format(new Date(e.at))}</p></div>)}</div>:<p className={styles.empty}>None yet.</p>}</section><section className={styles.section}><h2>Reset</h2><p>Deletes the ID, nickname, sound setting, and history stored in this browser. Submitted patterns stay in the global totals.</p><button className={`${styles.button} ${styles.danger}`} onClick={()=>{if(confirm("Delete local Untraced data?")){["untraced-player-id","untraced-history","untraced-nickname","untraced-muted"].forEach(k=>localStorage.removeItem(k));location.reload()}}}>Delete local data</button></section></>}
