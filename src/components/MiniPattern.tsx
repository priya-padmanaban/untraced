export default function MiniPattern({route,label}:Readonly<{route:number[];label?:string}>){const p=(n:number)=>({x:((n-1)%3)*40+10,y:Math.floor((n-1)/3)*40+10});return <svg className="miniPattern" viewBox="0 0 100 100" role="img" aria-label={label??"A discovered route"}><polyline points={route.map(n=>`${p(n).x},${p(n).y}`).join(" ")}/>{[1,2,3,4,5,6,7,8,9].map(n=><circle key={n} cx={p(n).x} cy={p(n).y} r="2.3"/>)}</svg>}
