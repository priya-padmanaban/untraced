export type Progress={discovered:number;totalSubmissions:number;browsers:number;huntStartedAt:string;completedAt:string|null;recent:PublicPattern[];popular:PublicPattern[];milestones:{threshold:string;discoveredCount:number;reachedAt:string}[]};
export type PublicPattern={route:number[];count:number;ordinal:number;firstDiscoveredAt:string;name:string|null;entryAt?:string;wasFirstDiscovery?:boolean};
export type SubmitResult={shareId?:string;isNewDiscovery:boolean;ordinal:number|null;submissionCount:number;firstDiscoveredAt:string;firstDiscovererName:string|null;discoveredTotal:number;totalSubmissions:number;personallySubmittedBefore:boolean;personal:{attempts:number;discoveries:number;distinct:number;selfRepeats:number;currentStreak:number;bestStreak:number};completedAt:string|null};
export type SharedResult={shareId:string;route:number[];wasFirstDiscovery:boolean;discoveryOrdinal:number;routeSubmissionCount:number;personalStreak:number;personallySubmittedBefore:boolean;submittedAt:string;firstDiscovererName:string|null};
