import { bigint,boolean,index,integer,pgTable,text,timestamp,uniqueIndex,uuid } from "drizzle-orm/pg-core";
export const patterns=pgTable("patterns",{routeKey:text("route_key").primaryKey(),firstDiscoveredAt:timestamp("first_discovered_at",{withTimezone:true}).notNull().defaultNow(),firstDiscovererId:uuid("first_discoverer_id"),firstDiscovererName:text("first_discoverer_name"),discoveryOrdinal:integer("discovery_ordinal").notNull(),submissionCount:bigint("submission_count",{mode:"number"}).notNull().default(1),lastSubmittedAt:timestamp("last_submitted_at",{withTimezone:true}).notNull().defaultNow()},t=>[uniqueIndex("patterns_ordinal_uq").on(t.discoveryOrdinal),index("patterns_last_idx").on(t.lastSubmittedAt)]);
export const submissions=pgTable("submissions",{id:uuid("id").defaultRandom().primaryKey(),routeKey:text("route_key").notNull().references(()=>patterns.routeKey),submittedAt:timestamp("submitted_at",{withTimezone:true}).notNull().defaultNow(),playerId:uuid("player_id").notNull(),wasFirstDiscovery:boolean("was_first_discovery").notNull()},t=>[index("submissions_time_idx").on(t.submittedAt),index("submissions_player_idx").on(t.playerId)]);
export const huntState=pgTable("hunt_state",{id:integer("id").primaryKey(),huntStartedAt:timestamp("hunt_started_at",{withTimezone:true}).notNull().defaultNow(),completedAt:timestamp("completed_at",{withTimezone:true}),finalRouteKey:text("final_route_key"),finalDiscovererName:text("final_discoverer_name"),nextOrdinal:integer("next_ordinal").notNull().default(1)});
export const milestones=pgTable("milestones",{threshold:text("threshold").primaryKey(),discoveredCount:integer("discovered_count").notNull(),reachedAt:timestamp("reached_at",{withTimezone:true}).notNull().defaultNow()});
